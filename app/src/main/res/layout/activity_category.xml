<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".categories.CategoryActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/dashboard_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?android:actionBarSize"
        android:theme = "@style/ThemeOverlay.AppCompat.Light"
        android:background="@color/colorPrimary" />

    <!-- prefixy jsou zbytecne a nekdy muzou i skodit, napriklad pokud si budes linear layouty pojmenovavat
         s prefixem ll (llFoo, llBar atd.), muze se ti stat, ze budes casem potrebovat layout zmenit na relative
         a vsechny vyskyty budes muset prejmenovavat, aby to nematlo
         -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_dashboard"
        android:layout_below="@id/dashboard_toolbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_dashboard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"

        android:src="@drawable/ic_plus"
        android:backgroundTint="@color/colorPrimary" />

</RelativeLayout>